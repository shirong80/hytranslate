{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "공통 capability — IPC + global shortcut + clipboard write + autostart + dialog (history export). 외부 URL(Ollama 다운로드)은 백엔드 고정 URL 명령으로만 연다. clipboard read 는 paste UI 가 있는 popup/menubar 만 허용 (code-review v1 follow-up §21).",
  "platforms": ["macOS"],
  "windows": ["main", "popup", "menubar"],
  "permissions": [
    "core:default",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-is-registered",
    "clipboard-manager:allow-write-text",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled",
    "dialog:allow-save"
  ]
}
