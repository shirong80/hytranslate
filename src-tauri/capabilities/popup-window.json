{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "popup-window",
  "description": "popup 윈도우만 자기 창 헤더(data-tauri-drag-region)를 드래그하도록 허용. 크기 변경과 top-left 재고정은 전용 app command resize_popup(Rust)이 처리하므로 generic window setter 권한은 두지 않는다. set-size/set-position 은 호출자가 label 로 다른 윈도우(main/menubar)를 지목할 수 있어(plugin window get_window) 최소 권한에서 제외한다.",
  "platforms": ["macOS"],
  "windows": ["popup"],
  "permissions": ["core:window:allow-start-dragging"]
}
